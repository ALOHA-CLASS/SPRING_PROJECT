<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aloha.shop.shop.mapper.ProductsMapper">

    <!-- 목록 -->
    <select id="list" resultType="Products">
        SELECT *
        FROM products
    </select>

    <!-- 조회 -->
    <select id="select" resultType="Products">
        SELECT *
        FROM products
        WHERE id = #{id}
    </select>

    <!-- 등록 -->
    <insert id="insert">
        INSERT INTO products ( id, name, category, content, price, stock )
        VALUES ( UUID(), #{name}, #{category}, #{content}, #{price}, #{stock} )
    </insert>

    <!-- 수정 -->
    <update id="update">
        UPDATE products
           SET name = #{name}
              ,content = #{content}
              ,price = #{price}
              ,stock = #{stock}
              ,updated_at = now()
        WHERE id = #{id}
    </update>

    <!-- 삭제 -->
    <delete id="delete">
        DELETE FROM products
        WHERE id IN ( ${id} )
    </delete>

</mapper>